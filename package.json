{
  "name": "pebbers-cdn",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "turbo": "^2.9.12"
  }
}
